{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability set for the SQLRite Journal main window. Locked down to core window/event APIs plus the dialog plugin (used for Open/Save-As/Export pickers). No fs, shell, http, or other capabilities are granted — every disk write goes through the Rust backend on top of the SQLRite engine.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:default",
    "core:event:default",
    "dialog:default"
  ]
}
